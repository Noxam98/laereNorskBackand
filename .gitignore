.venv/
__pycache__/
*.pyc
users.db
# -shm/-wal тоже: локальный WAL, приехавший rsync'ом рядом с прод-базой, SQLite попытается
# накатить на ЧУЖОЙ users.db при старте (привязки WAL к файлу БД нет) → порча прода.
users.db-shm
users.db-wal
app.log
.env

# покрытие тестов (локально)
.coverage
htmlcov/

# Рабочие заметки/дампы ревью и планов (scratch) — не для репозитория
REVIEW_a1_*
PLAN_a1_*
audit-findings.json
verb-adj-grammar-spec.json
